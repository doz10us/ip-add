if [[ $SSH_CONNECTION ]]; then
	sship=$(echo $SSH_CONNECTION | awk -F " " '{ print $1 }')
	cat .ip-add.conf | while read file
	do
		result=$(grep -Fc "$sship" $file)
		if [[ $result = "0" ]]; then
			echo "allow from "$sship >> $file
		fi
	done
fi
